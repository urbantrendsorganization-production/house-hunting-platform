<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The INTERNET permission is required for development. Specifically,
         the Flutter tool needs it to communicate with the running application
         to allow setting breakpoints, to provide hot reload, etc.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Debug-only: allow cleartext HTTP so the app can reach a local dev API
         over http:// (e.g. http://10.0.2.2:8000 from the Android emulator).
         Merged into debug builds only — release builds keep Android's default
         cleartext block, so production stays HTTPS-only. -->
    <application android:usesCleartextTraffic="true" />
</manifest>

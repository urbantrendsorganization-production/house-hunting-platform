# ops — Deploy & infrastructure

Placeholder for deploy configs (Caddy, prod compose, backup scripts). Fleshed
out in **Phase 7** (Hetzner prod behind Caddy, pg_dump + object storage backups).

Local dev uses the root `docker-compose.yml`, not this directory.
